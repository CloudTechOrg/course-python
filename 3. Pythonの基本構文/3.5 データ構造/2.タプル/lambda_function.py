def lambda_handler(event, context):
    # ------------------------------
    # タプルの使い方
    # ------------------------------
    my_tuple = (1, 2, 3, 4, 5) # タプルの定義
    print(my_tuple) # 出力 1, 2, 3, 4, 5

    for value in my_tuple:
        print(value) # 出力 1, 2, 3, 4, 5

    # ------------------------------
    # 途中で値を更新（エラーになる）
    # ------------------------------
    my_tuple = (1, 2, 3, 4, 5)
    # my_tuple[0] = 9 # エラーになる